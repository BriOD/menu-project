<h1><%= @category.name %>:</h1> <%= link_to "Edit", edit_category_path %>

  <% @category.items.each do |item| %>
  <p>  <%= item.name %> -- <%= item.price %> </p>
  <p>  -<%= item.description %></p>

  <% end %>
