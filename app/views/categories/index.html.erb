<h1>Here's  list of all the categories:</h1>

<ul>
  <% @categories.each do |category| %>
  <li><%= link_to category.name, category_path(category) %>   <%= link_to "Delete Category", category_path(category), method: "delete" %>
</li>

  <% end %>
</ul>
