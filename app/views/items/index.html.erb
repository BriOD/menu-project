<h1>All of the available items on the menu:</h1>

<% @items.each do |item| %>
  <ul>
    <li><%= link_to item.name, item_path(item) %>-<%= link_to "Edit #{item.name}", edit_item_path(item) %>-
      <%= link_to "Delete #{item.name}", item_path(item), method: "delete"  %></li>
  </ul>

<% end %>
